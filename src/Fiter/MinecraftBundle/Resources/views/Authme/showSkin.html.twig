{% extends '::base.html.twig' %}
 
{% block title %}Minecraft - {{ entity.username }} - {{ app.request.host }}{% endblock %}
 
{% block main %}

<section style="" class="paginador">
    <ul>
        {% if paginador.currentPage > 1 %}
            <li><a href="{{ path('authme_show_skin', { 'page': paginador.previousPage}) }}">Anterior</a></li>
        {% endif %}

        {% for page in paginador.minPageInRange..paginador.maxPageInRange %}
            {% if page == paginador.currentPage %}
                <li class="active"><a href="#">{{ page }}</a></li>
            {% else %}
                <li><a href="{{ path('authme_show_skin', { 'page': page }) }}">{{ page}}</a></li>
            {% endif %}
        {% endfor %}

        {% if paginador.currentPage < paginador.lastPage %}
            <li><a href="{{ path('authme_show_skin', { 'page': paginador.nextPage })}}">Siguiente</a></li>
        {% else %}
            
        {% endif %}
    </ul>
</section>
<div style="clear:both"></div>

{% include 'FiterMinecraftBundle:Authme:parcial/search.html.twig' %}

<a href="{{ path('authme_show', { 'id': entity.id }) }}">{{ entity.username }}</a><br/>
<div style="margin-right:.8em;width:53px;height:53px;float:left">
    <img src="{{ path('authme_skin', { 'id': entity.id, 't' : '1' }) }}" />
</div>
{% include 'FiterMinecraftBundle:Authme:parcial/datos.html.twig' %}
<div style="clear:both"></div>
{#% include 'FiterMinecraftBundle:Authme:parcial/skinPreview.html.twig' %#}
<a href="{{ path('authme_skin', { 'id': entity.id, 't' : '0' }) }}" type="application/octet-stream" download>
    <img src="{{ path('authme_skin', { 'id': entity.id, 't' : '0' }) }}">
</a>
<link rel="stylesheet" type="text/css" href="/bundles/fiterdefault/css/bootstrap.min.css" />
{#<a href="http://www.minecraft.net/profile/skin/remote?url=http://{{domain}}{{ path('authme_skin', { 'id': entity.id, 't' : '0' }) }}" class="btn btn-success">Ponte este skin</a>#}
<a href="http://www.minecraft.net/profile/skin/remote?url=http://{{domain}}/mcskin/img/{{entity.username}}.png" class="btn btn-success">Ponte este skin</a>



/mcskin/img/roijas.png

<a href="{{ path('authme_skin', { 'id': entity.id, 't' : '0' }) }}" type="application/octet-stream" download class="btn btn-success">{{ 'main.skinDownload' | trans }}</a><br/>
<div id="skinpreview"></div>
<h1></h1>

<script type="text/javascript" src="/mcskin/three.js"></script>
<script type="text/javascript">
	//var defaultImages = ['/mcskin/{{ entity.username }}.png'];
	//var defaultImages = ['/mcskin/img/{{ entity.username }}.png'];
	//var defaultImages = ['/mcskin/minecraft.php?u={{ entity.username }}'];
	var defaultImages = ["{{ path('authme_skin', { 'id': entity.id, 't' : '0' }) }}"];
</script>
<script type="text/javascript" src="/mcskin/bundle.js"></script>

<style type="text/css">
	#skinpreview input[type="checkbox"]{margin-right: .2em;vertical-align: initial;}
</style>

<script type="text/javascript" src="/js/formoid/jquery.uniform.min.js"></script>
<script type="text/javascript"> 
$(function () {
    $("form").find("input[type=file]").uniform();
    $(".uploader22 span.filename").text("No se ha seleccionado ningún archivo");
    $(".uploader22 span.action").text("Seleccionar"); 
    $(".uploader22 span.action").text("Seleccionar");
});

</script>
{% endblock %}


















