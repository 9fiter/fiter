<h1>Skillz creation</h1>

<form action="{{ path('skillz_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('skillz') }}">
            Back to the list
        </a>
    </li>
</ul>
