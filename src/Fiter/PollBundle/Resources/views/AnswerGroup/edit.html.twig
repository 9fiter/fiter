{% extends '::base.html.twig' %}
 
{% block title %}AnswerGroup edit{% endblock %}

{% block main %} 
    <h1>AnswerGroup edit</h1>
    <form action="{{ path('answergroup_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p><button type="submit">Edit</button></p>
    </form>
    <a class="boton" href="{{ path('answergroup') }}">Back to the list</a> 
    <form action="{{ path('answergroup_delete', { 'id': entity.id }) }}" method="post">
        {{ form_widget(delete_form) }}
        <button type="submit">Delete</button>
    </form>
{% endblock %}